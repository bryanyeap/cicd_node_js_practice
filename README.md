# node-app

Minimal Node.js + Express application.

## Setup

Install dependencies:

```powershell
cd node-app
npm install
```

Run:

```powershell
npm start
```

Dev (auto-restart):

```powershell
npm run dev
```

Test (quick script):

```powershell
npm test
```
